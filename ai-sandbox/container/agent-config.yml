eval:
  py: false
modelRoles:
  default: openrouter/meta/muse-spark-1.2-contributor:high
  smol: openrouter/deepseek/deepseek-v4-flash-0731
symbolPreset: nerd
hideThinkingBlock: true
# Built-in memory tools stay off; turn logging is handled by the postgres
# memory extension instead (see extensions/memory/).
memory:
  backend: "off"
setupVersion: 2
composer:
  shape: box
providers:
  # Exclude providers that fail in a sandboxed/datacenter environment:
  # - duckduckgo: bot-detection challenge on datacenter IPs
  # - google, ecosia, mojeek: require the shared browser daemon (Chromium),
  #   which is not available in the sandbox; they hang or time out.
  # Startpage (scraping fallback) remains the working default.
  webSearchOrder:
    - firecrawl
  webSearchExclude:
    - duckduckgo
    - google
    - ecosia
    - mojeek
edit:
  enabled: false
