# Providers for the sandbox. The apiKey is a placeholder -- the proxy
# overwrites the header. Never paste a real key here.
providers:
  neurawatt:
    baseUrl: https://api.neuralwatt.com/v1/
    api: openai-completions
    apiKey: dummy-replaced-by-proxy
    models:
      - id: kimi-k3
        name: Kimi k3
