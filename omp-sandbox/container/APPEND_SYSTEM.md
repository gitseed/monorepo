# File editing policy

The `edit` tool is disabled. For ALL file edits — creating, modifying, or
deleting files — use `morph_edit` (write JSON to `xd://morph_edit`).

Never call the `edit` tool. It will be blocked.
